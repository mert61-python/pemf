# =============================================================================
# PEMF Backend — CI TEST bağımlılıkları (audit B-3.3: CI 'requirements-test.txt'e
# referans veriyordu ama dosya YOKtu → pytest hiç kurulmuyor, hat asla yeşile dönmüyordu).
# -----------------------------------------------------------------------------
# Testlerin (guii/tests) IMPORT + ÇALIŞMA zamanında gerçekten gereken paketler.
# AĞIR AI bağımlılıkları (torch/ultralytics/onnx/librosa/numba/scikit-learn/xgboost)
# model çalıştırma anında LAZY yüklenir → import edilen modüller için GEREKMEZ, CI'dan
# çıkarıldı (kurulum ~2GB + dakikalarca sürerdi). sqlcipher3 de HARİÇ: ubuntu'da wheel
# zor + kod onsuz gracefully düz-metine düşer (import_sqlcipher()→None); testler bunu mock'lar.
# Sürümler ana requirements.txt (CPython 3.10) ile birebir kilitli.
# Kurulum (CI):  pip install -r requirements-test.txt
# =============================================================================

# ── Test koşucu + HTTP istemci (FastAPI TestClient httpx kullanır) + kapsam (audit B-3.1) ──
pytest==9.1.1
httpx==0.28.1
pytest-cov==7.1.0

# ── Web/API (api_server + router'ları import etmek için) ──
# DO-1: güvenlik-ilgili web deps'i shipped requirements.txt ile BİREBİR eşit (CI pip-audit gerçek
# prod ağacını tarasın + test/prod-parite; eskiden 1.2.1/0.0.30/41.0.3 = bayat → yanıltıcı CVE taraması).
fastapi==0.136.3
starlette==1.3.1
pydantic==2.13.4
python-multipart==0.0.31
# ⚠️ `backend_service.py` MODÜL DÜZEYİNDE `import uvicorn` yapar (satır 21). Eksik olduğu için
# onu import eden 24 test CI'da `ModuleNotFoundError` ile düşüyordu ve hat bir kez bile yeşile
# dönmemişti (2026-08-12'de teşhis edildi). Ağır AI paketleri gibi lazy DEĞİL — import zamanında
# gerekir. `[standard]` ekstraları CI'da gereksiz; sade paket yeterli, sürüm shipped ile eşit.
uvicorn==0.48.0

# ── Import-zamanı runtime bağımlılıkları (introspeksiyonla saptandı) ──
numpy==1.26.4
opencv-python-headless==4.11.0.86  # CI headless: opencv-python yerine (libGL gerektirmez).
                                   # SÜRÜM üretimle aynı (4.11.0.86) — yalnız varyant farklı; testler
                                   # prod'dan farklı bir cv2 API'siyle koşmasın diye hizalandı.
pillow==12.3.0        # CVE fix (denetim 2026-08-04): 12.2.0'da 26 acik — yigin OOB-write/decompression-bomb; ai_router kullanici yuklemesini dogrudan PIL.Image.open'a veriyor ve o uclar auth-muaf
reportlab==4.0.7                   # history_router → pdf_report_generator import-zamanı
cryptography==50.0.0               # DO-1: shipped ile esit (2026-08-04: 46.0.7->50.0.0 CVE bump)
keyring==25.7.0                    # sqlcipher_util opsiyonel import eder (yoksa graceful)
paho-mqtt==2.1.0                   # _mqtt_publish/_start_mqtt_for_api (graceful ama davranış eşleği)
zeroconf==0.149.16                 # DO-1: ağ-açık mDNS (LAN keşif) — testler import etmese de pip-audit KAPSAMINA al (CVE geçmişi; shipped ile eşit)
imageio-ffmpeg==0.6.0              # ses cozme zinciri (ai_router cat_sound): HASTA-GORUNUR sessizlik
                                   # kapisi (test_ses_sessiz_kayit_reddi, 3 test) bu paket yokken CI'da
                                   # ATLANIYORDU ve uc bir kez 500 dondu (kosu 31938268657, 2026-08-16).
                                   # "Agir AI" listesinde DEGIL (~30 MB, model kosturmaz); surum shipped
                                   # requirements.txt:71 ile birebir.
# ── XAI Faz 1 (2026-08-26, kosu 32947175192 dersi): test_xai_vendor_sertlestirme
# matplotlib/shap importunda ModuleNotFoundError ile CI'da KIRMIZI verdi (yerelde yesildi).
# Bunlar "agir AI" DEGIL (torch/model kosturmaz; toplam ~50 MB): ai_hub/xai_tabular
# sensitivity/SHAP + xai_utils overlay/rapor testleri gercek kutuphaneyle kosar.
# Surumler shipped requirements.txt ile birebir. imageio-ffmpeg dersinin ayni politikasi:
# test-kapsami sessizce atlanmasin diye kur, importorskip'e DUSME.
matplotlib==3.10.9
shap==0.49.1
slicer==0.0.8
pandas==2.3.3
scikit-learn==1.7.2
onnxruntime==1.19.2               # EM modulleri (em_kedi/fantom/petri) modul-seviyesinde ister;
                                   # XAI kalem-4 testleri gercek predict-zinciri sozlesmesini olcer
                                   # (CPU wheel ~50MB, GB-sinifi 'agir AI' degil; kosu 71c19a8 dersi)
