# AI System Requirements
# Install with: pip install -r requirements_ai.txt

# ==================== DEEP LEARNING FRAMEWORK ====================
onnxruntime

# ==================== BIOSIGNAL PROCESSING ====================
biosppy>=2.2.0          # Biosignal processing (ECG, R-peak detection)
wfdb>=4.1.0             # PhysioNet WFDB format reader
neurokit2>=0.2.12       # Advanced biosignal analysis (PRIMARY TOOL - replaces many others)
heartpy>=1.2.7          # Heart rate variability analysis (backup to NeuroKit2)

# ==================== SIGNAL PROCESSING ====================
scipy>=1.11.0           # Signal filtering, spectral analysis
numpy>=1.24.0           # Numerical computations
pywavelets>=1.4.1       # Wavelet transforms for signal decomposition

# ==================== DATA HANDLING ====================
pandas>=2.0.0           # Data manipulation and analysis
h5py>=3.9.0             # HDF5 file format for large datasets
tables>=3.8.0           # PyTables for hierarchical datasets

# ==================== DATA ACQUISITION ====================
requests>=2.31.0        # HTTP requests for dataset downloads
beautifulsoup4>=4.12.0  # Web scraping if needed
urllib3>=2.0.0          # URL handling

# ==================== MACHINE LEARNING UTILITIES ====================
scikit-learn>=1.3.0     # ML utilities, preprocessing, metrics
imbalanced-learn>=0.11.0  # Handle imbalanced datasets
optuna>=3.3.0           # Hyperparameter optimization

# ==================== VISUALIZATION ====================
matplotlib>=3.7.0       # Plotting and visualization
seaborn>=0.12.0         # Statistical visualizations
plotly>=5.17.0          # Interactive plots for reports

# ==================== PROGRESS TRACKING ====================
tqdm>=4.66.0            # Progress bars for downloads and training

# ==================== CONFIGURATION & UTILITIES ====================
pyyaml>=5.4.0,<6.0.0    # YAML configuration files (compatible with amqtt)
python-dotenv>=1.0.0    # Environment variable management
joblib>=1.3.0           # Parallel processing and caching

# ==================== LOGGING & MONITORING ====================
tensorboard>=2.14.0     # Training visualization
wandb>=0.15.0           # Experiment tracking (optional)

# ==================== MEDICAL DATA STANDARDS ====================
pydicom>=2.4.0          # DICOM medical imaging format (if needed)
nibabel>=5.1.0          # NIfTI format support (optional)

# ==================== TIME SERIES ANALYSIS ====================
statsmodels>=0.14.0     # Statistical models for time series
tslearn>=0.6.0          # Time series clustering and classification

# ==================== REPORT GENERATION ====================
jinja2>=3.1.2           # HTML template rendering
weasyprint>=59.0        # HTML to PDF conversion
reportlab>=4.0.0        # PDF generation

# ==================== DATABASE INTEGRATION ====================
# Already in main requirements: sqlite3 (built-in)
# sqlalchemy>=2.0.0     # If needed for advanced DB operations

# ==================== OPTIONAL: GPU ACCELERATION ====================
# numba>=0.57.0         # JIT compilation for numerical functions
# cupy>=12.0.0          # NumPy-compatible GPU array library (CUDA only)

# ==================== DEVELOPMENT & TESTING ====================
pytest>=7.4.0           # Unit testing
pytest-cov>=4.1.0       # Coverage reports
black>=23.7.0           # Code formatting
flake8>=6.1.0           # Linting
mypy>=1.5.0             # Type checking

# ==================== NOTES ====================
# COMPATIBILITY NOTES:
# - PyYAML: Using <6.0.0 for amqtt compatibility (amqtt requires <6.0.0)
# - Spyder: Requires PyQt5, but this project uses PyQt6
#   If you need Spyder IDE, install separately: pip install spyder pyqt5 pyqtwebengine
#   Or use VSCode/PyCharm for development
#
# For CUDA 12.3 support (your system - cuDNN 9.14), install PyTorch with CUDA 12.1:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# Note: PyTorch CUDA 12.1 wheels are forward-compatible with CUDA 12.3
# cuDNN 9.x is automatically included in PyTorch 2.x (no separate install needed)
#
# For CUDA 11.8 (if you need older version):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# For CPU only:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# For Apple Silicon (M1/M2):
#   pip install torch torchvision torchaudio
#   (MPS acceleration included automatically)
#
# Verify CUDA installation after install:
#   python -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA available: {torch.cuda.is_available()}'); print(f'CUDA version: {torch.version.cuda}'); print(f'Device: {torch.cuda.get_device_name(0) if torch.cuda.is_available() else \"CPU\"}')"
