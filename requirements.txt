# =============================================================================
# PEMF Headless Backend — Python bağımlılıkları  (TEK, KİLİTLİ kaynak dosya)
# -----------------------------------------------------------------------------
# Projenin DERLENMESİ (frozen EXE) + ÇALIŞMASI için gereken TÜM doğrudan
# bağımlılıklar. Sürümler, kanıtlanmış build ortamına (myenv, CPython 3.10)
# birebir KİLİTLİDİR (==). Eski requirements-service/-test/.lock dosyalarının
# YERİNİ ALIR — kafa karıştıran çoklu dosya kaldırıldı.
#
# Kurulum (TAM CPython 3.10 venv içine — embeddable python DEĞİL):
#   python -m pip install -r requirements.txt
#
# React'e geçildi → Qt/PyQt YOK.  QR eşleştirme KALDIRILDI → qrcode YOK.
# El-takibi söküldü → mediapipe YOK.  Eski eğitim/ECG kütüphaneleri (wfdb,
# neurokit2, heartpy, biosppy, statsmodels, tslearn, h5py, tables) YOK.
# =============================================================================

# torch CPU tekerlekleri PyPI'de yok; bu ek index'ten gelir:
--extra-index-url https://download.pytorch.org/whl/cpu

# ── Web / API (FastAPI servis + WebSocket) ──
fastapi==0.136.3
uvicorn[standard]==0.48.0
starlette==1.3.1                 # CVE fix (2026-07-06): 1.2.1→1.3.1 (FastAPI>=0.46 uyumlu, 86 test yeşil)
pydantic==2.13.4
python-multipart==0.0.31        # ai_router UploadFile/Form (runtime ŞART) — CVE fix: 0.0.30→0.0.31
websockets==15.0.1

# ── Donanım / iletişim (STM32 serial + ESP MQTT + mDNS keşfi) ──
paho-mqtt==2.1.0
pyserial==3.5
zeroconf==0.149.16              # CVE fix: 0.147.3→0.149.0→0.149.16 (SEC-8 2026-07-12: 5 mDNS CVE, minor-içi güvenli bump)

# ── AI/ML — görüntü + çekirdek ──
numpy==1.26.4                    # KRİTİK: torch 2.1.2 numpy<2 ister (>=2.0 KIRAR)
# opencv-python (full) — headless DEĞİL, BİLİNÇLİ. ultralytics `opencv-python`i SERT bağımlılık
# olarak ister; eskiden burada `opencv-python-headless==4.9.0.80` pinliyken pip ultralytics için
# full opencv'yi de kuruyordu (PIN'SİZ) ve `import cv2` FULL olanı çözüyordu. Sonuç: pin'lenen
# sürüm ile PAKETLENEN sürüm farklıydı (doğrulandı: bundle'da 4.11.0.86) → build tekrarlanabilir
# değildi, yeni bir opencv çıkınca ne shiplendiği sessizce değişirdi. Artık sahada çalışan sürüm
# AÇIKÇA pinli; headless satırı kaldırıldı (nasılsa gölgeleniyordu). Linux'ta libGL gerekir —
# Dockerfile.backend/ai zaten libgl1+libglib2.0-0 kuruyor.
opencv-python==4.11.0.86
onnxruntime==1.19.2
# CVE ERTELENDİ (bilinçli, 2026-07-06): onnx 1.15→1.16+ / torch 2.1.2→2.6+ CVE'leri PİNLİ bırakıldı —
# torch upgrade `numpy<2` kısıtını kaldırır → opencv/scipy/sklearn/librosa/pandas/numba CASCADE +
# 8 AI modelin klinik-çıktısı yeniden-doğrulanmalı (bu ortamda yapılamaz). Ayrı maintainer görevi.
onnx==1.15.0                     # ONNX model meta/graph (ultralytics export + inference)
torch==2.1.2+cpu
torchvision==0.16.2+cpu
ultralytics==8.4.47             # YOLO (landmark/segmentasyon/CT/retikülosit/petri)
pillow==12.2.0
scipy==1.15.3                    # torch/sklearn/librosa çekirdeği
scikit-learn==1.7.2             # CKD modeli PICKLE'dan yüklenir (statik import DEĞİL)
xgboost==3.2.0                   # cat_disease XGBoost.pkl PICKLE (statik import DEĞİL)

# ── AI/ML — ses (kedi sesi sınıflandırma) ──
librosa==0.11.0
imageio-ffmpeg==0.6.0           # herhangi ses formatı → 22050Hz mono WAV
numba==0.61.2                    # librosa mel-spektrogram JIT (frozen'da sürüm kritik)
llvmlite==0.44.0
soundfile==0.13.1

# ── Veri / raporlama ──
pandas==2.3.3
matplotlib==3.10.9              # yalnız Agg backend — PDF/rapor grafikleri
reportlab==4.0.7                # PDF rapor üretimi
PyYAML==6.0.3                    # cabin_config (em_fantom/em_petri/cat_organ)

# ── DB / şifreleme / sırlar ──
cryptography==46.0.7           # CVE fix (2026-07-16): 43.0.1→46.0.7 (bundled-OpenSSL CVE'ler; yalnız Fernet API kullanılıyor=x509/TLS-yok→pratik-düşük ama pip-audit temiz; Fernet 43→46 cross-version decrypt DOĞRULANDI, 117 test yeşil)
sqlcipher3==0.6.2                # at-rest DB şifreleme (Windows'ta prebuilt wheel gerekir)
keyring==25.7.0
joblib==1.5.3                    # sklearn model yükleme

# ── Bulut (offline-first Supabase cihaz kaydı) ──
supabase==2.31.0

# ── Gözlemlenebilirlik (opsiyonel uzaktan hata-izleme — audit B-5.1) ──
# VARSAYILAN KAPALI (KVKK); yalnız PEMF_SENTRY_DSN set edilince aktif. EXE'ye gömülü ama dormant.
sentry-sdk==2.24.1

# ── Build (frozen EXE) ──
pyinstaller==6.20.0

# ── Test ──
pytest==9.1.1
httpx==0.28.1
