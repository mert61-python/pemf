# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
build/
dist/
*.egg-info/
.eggs/
eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Databases
database/*.db
database/*.db-journal
database/*.db-shm
database/*.db-wal
# Sablon (template) veritabanlarini HARIC tut (onlari repoda istiyoruz)
!database/*_template.db

# Logs
logs/
*.log

# Large files & AI Models
*.onnx
# Küçük PKL modelleri (XGBoost vb.) repoda kalmalı, sadece çok büyükleri exclude ediyoruz
# Eger belirli modeller indirelecekse, onnx'leri zaten sildik
# ai_hub/**/*.pkl
*.pt
*.pth

# Virtual environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDEs and Editors
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# PyInstaller Spec output (optional)
build_tools/Output/

# LatteKurulum binaries (Too large for basic git repo)
lattekurulum/VC_redist.x64.exe
lattekurulum/stsw-stm32102/VCP_V1.5.0_Setup_W8_x64_64bits.exe
dataset/

# ── Üretime-hazırlık eklemeleri (kaynak commit'i için) ──
# Sanal ortamlar — mevcut .venv/venv kuralı .venv-build'i kaçırıyordu
.venv-build/
.venv*/
myenv/

# Frontend/mobil AYRI Expo projeleri (C:\Users\merta\pf junction + guii kopyalari) — backend repo'da izlenmez
frontend/
frontend_temp/
pf/
mobile/
node_modules/

# Build/dağıtım çıktıları + büyük/irrelevant dizinler
buildPEMF/
builds/
mobile_builds/
release_assets/
lattekurulum/
ai_hub/PEMF_AI_Test_Girdileri/
# training_archive/ -> guii disina tasindi: ../ml_training_archive (ML egitim arsivi)
github_pemf/

# Çalışma/derleme artefaktları
*.apk
*.ipa
_p2boot.*
*.plain.bak
*.enc.tmp
*.db-wal
*.db-shm
*.db-journal

# SIRLAR — ASLA commit etme (Gmail App Password / Supabase key / şifreleme anahtarları)
config/config.json
data/config.json
config/credentials/
**/.pemf_key
**/.pemf_key_v2
**/.sqlcipher_key
**/.encryption_key
**/pemf_secrets.json
# Yedek kurtarma malzemesi: kod TÜM veritabanı anahtarlarını açar, zarf onları taşır.
**/KURTARMA-KODU.txt
**/kurtarma-zarfi.enc
**/.recovery_fp

# KOD İMZALAMA SIRLARI — Apple Developer ID / App Store Connect.
# Bunlar sızarsa ÜÇÜNCÜ ŞAHIS SİZİN ADINIZA imzalı macOS uygulaması dağıtabilir
# (Gatekeeper geçer, kullanıcı sizin ürününüz sanır). Sertifika iptali + yeniden
# notarization gerektirir. Kalıcı yeri: Keychain / parola yöneticisi / CI Secrets.
*.p8
*.p12
*.key
*.csr
apple-mac-cert/
AuthKey_*.p8
developerID_application.cer
**/api_token.txt

# Üretilen raporlar
architecture_report.json
models_size.json

# P0/dagitim (commit edilmez): ucuncu-parti binary + build log + sifreleme-migrate yedegi
bin/cloudflared/
build_exe_*.txt
*.plain.bak

# ── guii → pemf-update (KOD-ONLY push): tum bundle binary'leri + calistirilabilirler haric ──
bin/
*.exe
*.dll
*.pyd
*.bin
*.node
# Model binary'leri (kod DEGIL — agir)
*.pkl
*.joblib
*.h5
*.pb
*.tflite
*.safetensors
*.model
# Dokumanlar/raporlar/gorseller (kod DEGIL)
tübitak/
*.docx
*.pdf
*.pptx
*.xlsx

# Lint/tip araç önbellekleri (audit B-2.1)
.ruff_cache/
.mypy_cache/

# Rust launcher build ciktisi (launcher/) — 150 MB+, tamamen yeniden uretilebilir
launcher/target/

# Offline (çevrimdışı) dağıtım installer paketi — 3.5GB DiskSpanning, git dışı
offline dağıtım/

# DENETIM 2026-08-04 (P2): pemf-app-packages/ yayin cikti klasoru. `manifest.json` (3,5 KB)
# BILEREK izlenir — CI'daki cift-base/URL-pin regresyon kapilari onun uzerinde kosar; izlenmezse
# o testler temiz checkout'ta sessizce ATLANIR ve YESIL raporlanir (koruma sifira duser).
# Paketlerin KENDISI (yuzlerce MB - GB) ASLA izlenmemeli.
pemf-app-packages/*.zip
pemf-app-packages/*.bin
pemf-app-packages/*.exe

# Android release imzalama sirlari (2026-08-06) — ASLA depoya girmemeli
keystore.properties
*.jks
*.keystore

# Android imzalama anahtari YEDEGI (2026-08-06). guii tasinabilir olsun diye burada;
# ASLA depoya/pakete girmemeli. Kaybolursa uygulama bir daha guncellenemez.
keys/

# Kaynak sifreleme parolasi (2026-08-06) — ASLA depoya girmemeli
build_tools/_static_password.py
*.pyenc

# ── BUILD CIKTILARI ve AYRI DEPOLAR (2026-08-10) ───────────────────────────────────────────
# PEMF_BUILD/: PyInstaller frozen agaci — 3,5 GB, her build'de yeniden uretilir. Kaynagi
# `scripts/build_backend_exe.ps1`; depoda tutmanin hicbir degeri yok, klonu imkansizlastirir.
PEMF_BUILD/

# macOS base paketi (654 MB / sikistirilmis 261 MB) — GitHub Release varligi, depo degil.
base-mac.zip
base-mac.sha256.txt

# pemf-vet-web/ KENDI GIT DEPOSUDUR (kendi .git'i + kendi Vercel projesi var). Ust depoya
# eklemek ya bozuk bir gitlink ya da 200 MB'lik node_modules demek olurdu; iki depo AYRI
# yonetilir ve AYRI yayinlanir (site: `npx vercel --prod`).
pemf-vet-web/

# NSIS BOM deneme kalintilari (tek seferlik hata ayiklama)
*_bom.nsi
ppo_bom.txt
