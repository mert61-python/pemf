<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" 
        tools:ignore="CoarseFineLocation" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" 
        tools:ignore="CoarseFineLocation" />
        
    <!-- BLE Permissions (Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"
        tools:targetApi="31" />
        
    <!-- Legacy BLE Permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- MQTT Service requires WAKE_LOCK to maintain connection when device is sleeping -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Foreground Service permission for MQTT service to run in background -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+ (API 34+) için foreground service type belirtilmesi gerekir -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    
    <!-- Android 13+ (API 33+) için dinamik BroadcastReceiver'lar için permission -->
    <permission
        android:name="${applicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        android:protectionLevel="signature" />
    <uses-permission android:name="${applicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" />

    <application
        android:name=".PemfApplication"
        android:allowBackup="true"
        android:icon="@drawable/pemf_heart_emf_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pemf_heart_emf_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.PEMFVetSystem"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PEMFVetSystem"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.system.SystemSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.PEMFVetSystem" />

        <!-- ✅ MQTT Foreground Service - Keeps connection alive in background -->
        <service
            android:name=".services.MqttForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        
        <!-- Removed MqttKeepAliveService - Redundant with Paho auto-reconnect -->
        
        <!-- MqttService is no longer needed - We use standard MqttClient instead of MqttAndroidClient -->
        <!-- This avoids AlarmPingSender Android 13+ compatibility issues -->
    </application>

</manifest>

