# PEMF AI Servisi (GPU) — Faz 2b bağımlılıkları (torch-cuda + ultralytics eklendi).
# onnxruntime-gpu → CUDA 12.x + cuDNN 9 (base imaj). torch → YOLO/ultralytics modelleri için
# (ultralytics import'u torch ister; .onnx inference'ı onnxruntime-gpu CUDA ile koşar).
# NOT: torch cudnn'i kendi bundle'ında; onnxruntime sistem cudnn9 kullanır — aynı süreçte
#      YOLO onnx torch-CUDA-op tetiklemediği için çakışma beklenmez (import-only torch).
onnxruntime-gpu==1.19.2
numpy==1.26.4
opencv-python==4.11.0.86          # requirements.txt / docker ile HİZALI (bkz. requirements.txt opencv notu)
pillow==12.3.0        # CVE fix (denetim 2026-08-04): 12.2.0'da 26 acik — yigin OOB-write/decompression-bomb; ai_router kullanici yuklemesini dogrudan PIL.Image.open'a veriyor ve o uclar auth-muaf
scipy==1.15.3
scikit-learn==1.7.2
xgboost==3.2.0                    # cat_disease (XGBoost.pkl)
shap==0.49.1                      # XAI Faz 1 (cat_disease TreeExplainer + CKD/EM kernel-SHAP)
slicer==0.0.8                     # shap bagimliligi
# XAI Faz 2 (2026-08-26): ses/termal Grad-CAM isi haritalari (/infer/sound|thermal explain=true).
# Pinler torch 2.1.2+cpu ile OLCULEREK dogrulandi; cu128/torch 2.7.1 imaj-ici smoke
# (docker build + /explain istegi) GPU makinesinde YAYIN ONCESI kosulmali — grad-cam/timm
# saf-Python + torch-API katmani oldugundan uyumsuzluk beklenmiyor ama olculmedi.
# ⚠️ grad-cam pip'te opencv-python-headless ceker; imajda opencv-python zaten var —
# Dockerfile.ai'de kurulum SIRASI/temizligi ayni cift-cv2 tuzagina dikkat.
grad-cam==1.5.7
timm==1.0.28
ttach==0.0.3
safetensors==0.8.0
captum==0.8.0
joblib==1.5.3
pandas==2.3.3
PyYAML==6.0.3
onnx==1.15.0
# ses (cat_sound)
librosa==0.11.0
numba==0.61.2
llvmlite==0.44.0
soundfile==0.13.1
# görüntü/YOLO (kidney_ct, segmentation, landmark, cat_organ, reticulocytes)
# torch/torchvision cu128 (RTX 5090/Blackwell) → Dockerfile.ai'de AYRI index'ten kurulur. Buraya EKLEME.
ultralytics==8.4.47
# web
fastapi==0.136.3
uvicorn[standard]==0.48.0
python-multipart==0.0.31
# Scratch/Yara-Kapanma CPN — /infer/scratch (2026-08-26, scratch-entegrasyon-plani.md).
# torch cu128 AYRI index'ten ONCE kurulur (Dockerfile.ai) → celldetection'in torch>=2.0
# istegi mevcut 2.7.1+cu128'i EZMEMELI (imaj-ici dogrulama smoke'ta). ⚠️ albumentations
# opencv-python-headless CEKER → cift-cv2: kurulum sonrasi headless kaldir +
# opencv-python==4.11.0.86 force-reinstall (grad-cam kuraliyla ayni).
celldetection==0.4.9
